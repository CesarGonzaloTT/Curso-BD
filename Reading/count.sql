--COUNT

--Cuenta el número de elementos que coinciden con la búsqueda, en este caso cuenta los usuarios que registraron su edad.
SELECT COUNT(age) FROM users;

--Cuenta el número de usuarios en la tabla.
SELECT COUNT(*) FROM users;